import styles from './styles.module.scss';

export function SubscribeButtton() {
  return (
    <button
      type="button"
      className={styles.subscribeButton}
    >
      Subscribe now
    </button>
  );
}
